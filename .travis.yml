language: python

python:
  - "3.4"
  - "3.5"
  - "3.5-dev" # 3.5 development branch
  - "3.6"
# command to install dependencies
install: "pip install nanolyse"
# command to run tests
script: python -c "import nanolyse"
